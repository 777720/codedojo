练习的项目：selected-tree
_.union(array)
用途：数组合并，并去除重复值，array是要合并的几个数组

_.indexOf(array,value,fromIndex)
用途：找到你要查找的值在这个数组中的索引，返回-1就是没有
array：要查找的数组
value：要查找的值
fromIndex：要从数组的哪里开始查找，不是必须

_remove(array,callback())
用途：移除数组中的元素
array：要移除元素的数组
callback：回调函数，回掉函数会遍历数组中每一个元素，当函数返回true的时候，移除该元素

_.flattenDeep(array)
用途：数组降维

_.difference(array,value)
用途：从数组中过滤元素
array：要过滤的数组
value：过滤的值
